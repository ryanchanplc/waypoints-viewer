import React from 'react'
import { LoadingDiv, Loader } from 'component/Loading.style'

const Loading = () => {
  return (
    <LoadingDiv>
      <Loader />
    </LoadingDiv>
  )
}
export default Loading
